# nginx/docker-compose.yml
version: "3"
services:
  nginx:
    container_name: 'nginx'
    image: nginx
    ports:
      - 8080:8080
#      - 80:80
#      - 443:443
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d